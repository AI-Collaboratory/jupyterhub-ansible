---
  - name: set up a jupyterhub
    hosts: stage
    become: true
    become_user: root
    become_method: sudo
    vars_prompt:
      - name: ssl_passphrase
        prompt: "Enter SSL certificate pass phrase"
        private: false
    roles:
      - system
      - python
      - jupyterhub
